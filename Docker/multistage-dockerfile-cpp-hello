FROM ubuntu:latest AS builder

WORKDIR /builder

RUN apt-get update && \
    apt-get install -y g++

COPY main.cpp .

RUN g++ main.cpp -o a.out

FROM ubuntu:latest

WORKDIR /app

COPY --from=builder /builder/a.out .

CMD ["./a.out"]
